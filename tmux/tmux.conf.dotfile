set-option -g default-command $SHELL

# Plugins
set -g @tpm_plugins '              \
  tmux-plugins/tpm                 \
  tmux-plugins/tmux-sensible       \
'

unbind C-b
set -g prefix F9
bind F9 send-prefix

run-shell '~/.tmux/plugins/tpm/tpm'

# vim:ft=tmux.conf
