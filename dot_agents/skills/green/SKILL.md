---
user-invocable: true
model-auto-invocable: false
---

Write the minimal production code to make the failing test(s) pass.

Steps:
1. Identify the failing test(s) from context (or ask if unclear)
2. Write the simplest code that makes them pass — no extras, no optimization
3. Run all the tests to confirm that they are all green
4. Report the passing test output

Rules:
- Minimal code only — no refactoring, no cleanup, no extra features
- If multiple tests are failing, fix them one at a time
- Stop as soon as all tests pass
